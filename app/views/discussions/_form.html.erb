<%= form_for(@discussion) do |f| %>
  <% if @discussion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@discussion.errors.count, "error") %> prohibited this discussion from being saved:</h2>

      <ul>
      <% @discussion.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :thesis %><br />
    <%= f.text_area :thesis %>
  </div>
  <div class="field">
    <%= f.label :argument %><br />
    <%= f.text_area :argument %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.number_field :name %>
  </div>
  <div class="field">
    <%= f.label :determination%><br />
    <%= f.number_field :determination %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
